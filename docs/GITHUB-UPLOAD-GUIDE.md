# 📤 راهنمای آپلود فایل‌ها در GitHub
# GitHub Upload Guide

راهنمای گام‌به‌گام برای آپلود فایل‌ها مستقیماً در GitHub (بدون نیاز به Git و Command Line)

---

## 📋 فهرست

- [چرا این روش؟](#-چرا-این-روش)
- [پیش‌نیازها](#-پیشنیازها)
- [روش 1: ایجاد فایل متنی](#-روش-1-ایجاد-فایل-متنی)
- [روش 2: آپلود فایل‌های JSON](#-روش-2-آپلود-فایلهای-json)
- [روش 3: ساخت فولدر](#-روش-3-ساخت-فولدر)
- [روش 4: ویرایش فایل موجود](#-روش-4-ویرایش-فایل-موجود)
- [نکات مهم](#-نکات-مهم)
- [مشکلات رایج](#-مشکلات-رایج)

---

## 🎯 چرا این روش؟

این راهنما برای کسانی است که:

- ✅ **اینترنت ضعیف** دارند
- ✅ **Git** نصب ندارند
- ✅ **Command Line** بلد نیستند
- ✅ از **موبایل** کار می‌کنند
- ✅ می‌خواهند **سریع** فایل اضافه کنند

---

## 🔧 پیش‌نیازها

- ✅ حساب کاربری GitHub
- ✅ مخزن ساخته شده
- ✅ مرورگر وب (Chrome, Firefox, Safari)
- ✅ اتصال به اینترنت (حتی ضعیف)

---

## 📝 روش 1: ایجاد فایل متنی

برای فایل‌های متنی مانند `.md`, `.txt`, `.py`, `.json`

### مراحل:

#### گام 1: رفتن به مخزن

1. وارد GitHub شوید
2. به مخزن خود بروید
3. به فولدر مورد نظر بروید (یا در ریشه بمانید)

#### گام 2: ساخت فایل جدید

1. کلیک روی **Add file** (بالای صفحه، سمت راست)
2. انتخاب **Create new file**

#### گام 3: نام‌گذاری فایل

در کادر بالا نام فایل را بنویسید:

**مثال‌ها:**
```
README.md
kpi_generator.py
requirements.txt
templates/kpi-template.json
docs/guide.md
```

⚠️ **نکته مهم:** با نوشتن `/` فولدر ساخته می‌شود!

**مثال:**
```
tools/README.md
```
این خودکار فولدر `tools/` می‌سازد و فایل `README.md` را داخل آن قرار می‌دهد.

#### گام 4: نوشتن محتوا

در کادر بزرگ محتوای فایل را بنویسید یا **کپی/پیست** کنید.

**نکات:**
- از **Ctrl+A** برای انتخاب همه استفاده کنید
- از **Ctrl+C** برای کپی
- از **Ctrl+V** برای پیست

#### گام 5: پیش‌نمایش (اختیاری)

برای فایل‌های Markdown:
1. کلیک روی تب **Preview**
2. بررسی فرمت و ظاهر

#### گام 6: Commit

پایین صفحه:

1. **Commit message** بنویسید:
```
   docs: Add new documentation file
   feat: Add KPI generator script
   fix: Update README with corrections
```

2. **توضیحات بیشتر** (اختیاری):
```
   - اضافه کردن راهنمای جامع
   - بهبود مستندات
```

3. **Commit directly** را انتخاب کنید

4. کلیک روی **Commit new file**

✅ **تمام!** فایل شما ساخته شد.

---

## 📊 روش 2: آپلود فایل‌های JSON

برای فایل‌های بزرگ JSON (مانند شاخص‌ها)

### مراحل:

#### گام 1: آماده‌سازی فایل

1. فایل JSON را در کامپیوتر خود بسازید
2. با Notepad یا VS Code ویرایش کنید
3. ذخیره کنید

#### گام 2: آپلود در GitHub

1. به مخزن بروید
2. به فولدر مورد نظر بروید
3. کلیک روی **Add file**
4. انتخاب **Upload files**

#### گام 3: انتخاب فایل

**روش A: Drag & Drop**
- فایل را بگیرید و در کادر رها کنید

**روش B: Browse**
- کلیک روی **choose your files**
- فایل را انتخاب کنید

#### گام 4: Commit

مانند روش 1، Commit message بنویسید و **Commit changes** کلیک کنید.

---

## 📁 روش 3: ساخت فولدر

GitHub فولدرهای خالی را نگه نمی‌دارد، پس باید فایلی داخلش باشد.

### مراحل:

#### روش A: با ساخت فایل

1. **Create new file** کلیک کنید
2. نام بنویسید: `folder-name/README.md`
3. محتوا اضافه کنید
4. Commit کنید

✅ فولدر `folder-name/` ساخته می‌شود!

#### روش B: با .gitkeep

برای فولدر خالی که بعداً پر می‌شود:

1. **Create new file**
2. نام: `folder-name/.gitkeep`
3. محتوا: خالی بگذارید
4. Commit کنید

---

## ✏️ روش 4: ویرایش فایل موجود

### مراحل:

#### گام 1: پیدا کردن فایل

1. به فایل مورد نظر بروید
2. فایل را باز کنید

#### گام 2: ویرایش

1. کلیک روی آیکون **مداد (Edit)** - بالای فایل سمت راست
2. تغییرات را اعمال کنید

#### گام 3: Commit تغییرات

پایین صفحه:

1. Commit message: `fix: Update README with new information`
2. **Commit changes** کلیک کنید

---

## 💡 نکات مهم

### نام‌گذاری فایل‌ها

✅ **درست:**
```
README.md
kpi_generator.py
requirements.txt
R6-1-003-customer-retention.json
```

❌ **اشتباه:**
```
فایل من.txt       ← فاصله و فارسی
My File.doc        ← فاصله
test?.json         ← کاراکتر غیرمجاز
```

**قوانین:**
- بدون فاصله (از `-` یا `_` استفاده کنید)
- بدون کاراکترهای فارسی
- بدون کاراکترهای خاص: `? * : < > | " \`
- حروف کوچک توصیه می‌شود

---

### Commit Messages

**فرمت استاندارد:**
```
<type>: <subject>

<body> (اختیاری)
```

**انواع Type:**

| Type | کاربرد | مثال |
|------|--------|------|
| `feat` | ویژگی جدید | `feat: Add KPI generator` |
| `fix` | رفع باگ | `fix: Correct formula in R6-1-003` |
| `docs` | مستندات | `docs: Update README` |
| `style` | فرمت کد | `style: Format JSON files` |
| `refactor` | بازنویسی کد | `refactor: Improve code structure` |
| `test` | تست | `test: Add unit tests` |
| `chore` | کارهای عمومی | `chore: Update dependencies` |

**مثال‌های خوب:**
```
feat: Add customer retention KPI (R6-1-003)
docs: Add complete project structure guide
fix: Correct SMART score calculation
refactor: Reorganize kpi-library folder structure
```

**مثال‌های بد:**
```
update           ← خیلی کلی
فایل جدید        ← فارسی
asdfgh          ← بی‌معنی
.               ← بدون توضیح
```

---

### ساختار فولدر

**مسیرهای صحیح:**
```
templates/kpi-passport-template.json          ✅
kpi-library/criterion-6.../6-1-.../R6-1-003.json    ✅
docs/PROJECT-STRUCTURE.md                     ✅
```

**اشتباهات رایج:**
```
templates\kpi-template.json     ❌ (backslash در Windows)
/templates/file.json            ❌ (شروع با /)
templates//file.json            ❌ (دو بار /)
```

---

## 🐛 مشکلات رایج

### مشکل 1: فایل آپلود نمی‌شود

**علت‌های ممکن:**
- حجم بیش از 25 MB
- فرمت پشتیبانی نمی‌شود
- اتصال اینترنت قطع شد

**راه‌حل:**
1. حجم فایل را چک کنید
2. فایل را فشرده کنید
3. اینترنت را چک کنید
4. دوباره تلاش کنید

---

### مشکل 2: فولدر ساخته نمی‌شود

**علت:**
- فقط نام فولدر نوشتید، بدون `/`
- فایلی داخل فولدر نگذاشتید

**راه‌حل:**
- حتماً `/` بگذارید: `folder/file.md`
- یک فایل داخل فولدر بسازید

---

### مشکل 3: تغییرات اعمال نمی‌شود

**علت:**
- Commit نکردید
- مرورگر Cache دارد

**راه‌حل:**
1. حتماً **Commit changes** کلیک کنید
2. صفحه را Refresh کنید (F5)
3. Ctrl+F5 برای Hard Refresh

---

### مشکل 4: فایل فارسی درست نمایش نمی‌شود

**علت:**
- Encoding اشتباه است

**راه‌حل:**
1. فایل را با UTF-8 ذخیره کنید
2. در Notepad: Save As → Encoding: UTF-8

---

### مشکل 5: JSON خطا دارد

**علت:**
- فرمت JSON اشتباه است

**راه‌حل:**
1. از [JSONLint.com](https://jsonlint.com/) استفاده کنید
2. خطاها را برطرف کنید
3. دوباره آپلود کنید

---

## 📱 آپلود از موبایل

### Android / iOS

**مرحله 1:**
- از مرورگر (Chrome/Safari) وارد GitHub شوید
- گزینه "Desktop Site" را فعال کنید

**مرحله 2:**
- مانند راهنمای بالا عمل کنید
- برای فایل‌های کوچک: Create new file
- برای فایل‌های بزرگ: Upload files

**نکات:**
- ✅ ایجاد فایل متنی راحت است
- ✅ ویرایش فایل موجود آسان است
- ❌ آپلود فایل‌های بزرگ سخت است
- 💡 توصیه: از کامپیوتر استفاده کنید

---

## 🎓 توصیه‌های بهترین روش

### ✅ انجام دهید:

1. **قبل از Commit پیش‌نمایش کنید**
   - برای Markdown از Preview استفاده کنید
   - برای JSON از Validator استفاده کنید

2. **Commit Message واضح بنویسید**
   - توضیح دهید چه کاری کردید
   - از فرمت استاندارد استفاده کنید

3. **فایل‌ها را سازماندهی کنید**
   - در فولدر مناسب قرار دهید
   - از ساختار پروژه پیروی کنید

4. **قبل از آپلود تست کنید**
   - فایل JSON را Validate کنید
   - کد Python را تست کنید

---

### ❌ انجام ندهید:

1. **فایل‌های بزرگ آپلود نکنید**
   - حد مجاز: 25 MB
   - برای فایل‌های بزرگ از Git LFS استفاده کنید

2. **اطلاعات حساس آپلود نکنید**
   - رمز عبور
   - API Keys
   - اطلاعات شخصی

3. **Commit های بی‌معنی نزنید**
   - "update", "test", "asdf"
   - این‌ها تاریخچه را آلوده می‌کنند

4. **فایل‌های موقت آپلود نکنید**
   - `.DS_Store` (Mac)
   - `Thumbs.db` (Windows)
   - `__pycache__/` (Python)

---

## 🔐 امنیت

### محافظت از اطلاعات حساس

**✅ استفاده کنید:**
- `.env.example` برای نمونه تنظیمات
- `.gitignore` برای فایل‌های خصوصی

**❌ آپلود نکنید:**
- `.env` با API Keys واقعی
- فایل‌های پیکربندی با رمز
- دیتابیس با اطلاعات واقعی

**مثال .gitignore:**
```
# محیط
.env
*.env

# Python
__pycache__/
*.pyc

# دیتابیس
*.db
*.sqlite

# لاگ
*.log

# IDE
.vscode/
.idea/
```

---

## 📞 کمک بیشتر

اگر مشکلی دارید:

1. **GitHub Docs:** [docs.github.com](https://docs.github.com)
2. **پروژه ما:** [Issues](../../issues)
3. **ایمیل:** support@efqm-club.org

---

## ✅ چک‌لیست آپلود

قبل از Commit، این موارد را چک کنید:

- [ ] نام فایل صحیح است
- [ ] مسیر فولدر درست است
- [ ] محتوا کامل است
- [ ] فرمت صحیح است (JSON valid, etc.)
- [ ] Commit message واضح است
- [ ] اطلاعات حساس حذف شده
- [ ] پیش‌نمایش را دیدم

---

**نسخه:** 1.0.0  
**تاریخ:** 2025-01-15  
**نویسنده:** حمید اقتداریان

---

**[⬆ بازگشت به بالا](#-راهنمای-آپلود-فایلها-در-github)**
```

4. **Commit:** `docs: Add GITHUB-UPLOAD-GUIDE.md - راهنمای کامل آپلود در GitHub`

---

## 🎉 تبریک! همه فایل‌های docs/ کامل شد!

---

## 📊 خلاصه نهایی امروز:
```
✅ Root: 11 فایل
✅ templates/: 2 فایل
✅ kpi-library/: 4 فایل
✅ tools/: 1 فایل
✅ benchmarks/: 1 فایل
✅ data-models/: 1 فایل
✅ examples/: 1 فایل
✅ docs/: 4 فایل ⭐ (2 قبلی + 2 جدید)
✅ backend/: 1 فایل
✅ frontend/: 1 فایل
✅ database/: 1 فایل

📊 جمع: 28 فایل
🏆 وضعیت: 100% کامل!
